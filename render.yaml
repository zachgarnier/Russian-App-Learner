services:
  - type: web
    name: russian-tts-backend
    env: python
    plan: free
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "cd backend && gunicorn server:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
